<UserControl x:Class="ScreenDivider.Views.Controls.ArrowUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:local="clr-namespace:ScreenDivider.Converters"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" IsReversed="True" />
    </UserControl.Resources>
    <Grid HorizontalAlignment="Left" >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Image Name="Arrow" Grid.Column="0" Grid.Row="0" Source="/WPFTry;component/Resources/CircledArrowRight.png" Margin="10,10,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Path=IsCurrent, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
