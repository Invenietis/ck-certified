<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:vm="clr-namespace:CK.WordPredictor.UI.ViewModels"
    xmlns:h="clr-namespace:CK.WordPredictor.UI"
    x:Class="CK.WordPredictor.UI.TextualContextSmartAreaWindow"
    mc:Ignorable="d" Height="208" Width="539">
    <Grid>

        <TextBox Text="{Binding TextualContext, UpdateSourceTrigger=PropertyChanged}" MinLines="3" FontSize="15" FlowDirection="LeftToRight" Margin="10,10,105,10" AcceptsReturn="True" AcceptsTab="True" >
            <i:Interaction.Behaviors>
                <h:SetCaretIndexBehavior CaretPosition="{Binding CaretIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                         SelectedText="{Binding SelectedText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </i:Interaction.Behaviors>
        </TextBox>
        <Button Content="Envoyer" HorizontalAlignment="Right" Margin="10" VerticalAlignment="Stretch" Width="90" UseLayoutRounding="False" RenderTransformOrigin="0.5,0.5">
        </Button>
    </Grid>
</Window>
