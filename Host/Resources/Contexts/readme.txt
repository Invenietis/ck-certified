ContextCiviKeySmall : To use in debug when you want to have a small number of keys.
ContextCiviKey-before20130322 : The mouse keyboard has no images and doesn't have the drag n drop button
Context : the current one, with drag n drop on the mouse keyboard (and ugly images)