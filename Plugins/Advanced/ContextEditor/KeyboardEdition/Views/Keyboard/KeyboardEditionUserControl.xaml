<UserControl x:Class="ContextEditor.s.KeyboardEditionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctrls="clr-namespace:CK.WPF.Controls;assembly=CK.WPF.Controls"
             xmlns:ckwincore="clr-namespace:CK.Windows;assembly=CK.Windows.Core"
             xmlns:view="clr-namespace:CK.WPF.StandardViews;assembly=CK.WPF.StandardViews"
             xmlns:vm="clr-namespace:ContextEditor.ViewModels"
             xmlns:cce="clr-namespace:Microsoft.Windows.Controls;assembly=CK.WPF.Controls"
             xmlns:local="clr-namespace:ContextEditor.s"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            

            <!--Altered template of the stdKeyview-->
            <Style x:Key="MiniZoneDisplayedKeyStyle" TargetType="{x:Type view:StdKeyView}">
                <Setter Property="KeyPressedCommand" Value="{Binding KeyPressedCommand}"/>
                <Setter Property="TextDecorations" Value="{Binding TextDecorations}"/>
                <Setter Property="KeyDownCommand" Value="{Binding KeyDownCommand}"/>
                <Setter Property="KeyUpCommand" Value="{Binding KeyUpCommand}"/>
                <Setter Property="FontWeight" Value="{Binding FontWeight}"/>
                <Setter Property="DownLabel" Value="{Binding DownLabel}"/>
                <Setter Property="FontStyle" Value="{Binding FontStyle}"/>
                <Setter Property="ShowLabel" Value="{Binding ShowLabel}"/>
                <Setter Property="Visibility" Value="{Binding Visible}"/>
                <Setter Property="FontSize" Value="{Binding FontSize}"/>
                <Setter Property="UpLabel" Value="{Binding UpLabel}"/>
                <Setter Property="Opacity" Value="{Binding Opacity}"/>
                <Setter Property="CustomContent" Value="{Binding Image}" />

                <!-- Border brush -->
                <Setter Property="BorderBrush">
                    <Setter.Value>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FF999999" Offset="0"/>
                            <GradientStop Color="#FFBBBBBB" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <!-- All about font -->
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="FontWeight" Value="Normal"/>
                <Setter Property="FontStyle" Value="Normal"/>
                <Setter Property="FontFamily" Value="Arial"/>
                <Setter Property="Foreground" Value="Black"/>
                <!-- BackgroundBrush -->
                <Setter Property="Background" Value="White" />
                <Setter Property="HoverBackground" Value="White" />
                <Setter Property="PressedBackground" Value="White" />
                <!-- Template -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type view:StdKeyView}">
                            <Grid x:Name="buttonGrid">
                                <Border x:Name="Border" CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1.5"
                                Background="{TemplateBinding Background}" />
                                <Border CornerRadius="5" x:Name="GradientMask"
                                    BorderBrush="Transparent" BorderThickness="1.5">
                                    <Border.Background>
                                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="#24000000" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                                <TextBlock Padding="2" x:Name="Letter" Foreground="{TemplateBinding Foreground}" Text="{TemplateBinding UpLabel}" Visibility="Collapsed"
                                        HorizontalAlignment="Center" VerticalAlignment="Center"  TextDecorations="{TemplateBinding TextDecorations}"
                                        FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}" FontStyle="{TemplateBinding FontStyle}"/>
                                <ContentPresenter Margin="2" x:Name="Image" HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding CustomContent}" Width="32"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <!-- Trigger on : IsMouseOver -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="Black"/>
                                    <Setter TargetName="Border" Property="Background" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=HoverBackground}"/>
                                </Trigger>
                                <!-- Trigger on : IsPressed -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=PressedBackground}"/>
                                    <Setter TargetName="GradientMask" Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                <GradientStop Color="#24000000" Offset="0"/>
                                                <GradientStop Color="Transparent" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter TargetName="Letter" Property="Text" Value="{Binding RelativeSource={RelativeSource TemplatedParent},  Path=DownLabel}"/>
                                </Trigger>
                                <!-- Trigger on : ShowLabel -->
                                <Trigger Property="CustomContent" Value="{x:Null}">
                                    <Setter TargetName="Letter" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
        </ResourceDictionary>
    </UserControl.Resources>
    <Border BorderThickness="1" BorderBrush="Black" CornerRadius="5">
        <ItemsControl ItemsSource="{Binding Zones}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="20" CornerRadius="10" BorderThickness="1" BorderBrush="LightGray" Background="Ivory" Padding="5">
                        <DockPanel LastChildFill="True">
                            <ItemsControl DockPanel.Dock="Top" Width="300" Height="100" ItemsSource="{Binding Keys}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <view:StdKeyView Width="30" Height="30" Style="{StaticResource MiniZoneDisplayedKeyStyle}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <DockPanel Opacity="0" x:Name="zoneConfigurationPanel" DockPanel.Dock="Bottom" LastChildFill="False">
                                <Button DockPanel.Dock="Left" Margin="5" Width="50" Height="50" Command="{Binding DeleteZoneCommand}">
                                    <Image Width="24" Source="pack://application:,,,/ContextEditor;component/Resources/Images/delete.png" />
                                </Button>
                                <Button DockPanel.Dock="Right" Margin="5" Width="50" Height="50" Command="{Binding SelectZoneCommand}">
                                    <Image Width="24" Source="pack://application:,,,/ContextEditor;component/Resources/Images/edit-keyboard.png" />
                                </Button>
                            </DockPanel>
                            
                        </DockPanel>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard Name="showZoneConfiguration">
                                        <DoubleAnimation Storyboard.TargetName="zoneConfigurationPanel" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard Name="hideZoneConfiguration">
                                        <DoubleAnimation Storyboard.TargetName="zoneConfigurationPanel" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                    </Border>
                    
                </DataTemplate>

            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
